# .goreleaser.yml
version: 2
project_name: copilot-proxy
release:
  github:
    owner: dvcrn
    name: copilot-oauth-proxy
  draft: true
builds:
  - id: copilot-oauth-proxy
    main: ./cmd/copilot-oauth-proxy/
    binary: copilot-oauth-proxy
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
      - -buildvcs=false
    ldflags:
      - -s -w -buildid=
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      # For Apple Silicon (M1/M2)
      - arm64
archives:
  - formats:
      - tar.gz
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
checksum:
  name_template: "checksums.txt"
changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
